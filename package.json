{
  "name": "extrapotions-prisma",
  "version": "3.0.31",
  "private": true,
  "description": "PRISMA V3 local context-aware identity-language highlighter",
  "scripts": {
    "build": "node scripts/build.cjs",
    "build:check": "node scripts/build.cjs --check",
    "test": "node scripts/build.cjs && node --test tests/*.test.cjs",
    "release:check": "node scripts/release-check.cjs"
  },
  "devDependencies": {
    "playwright": "1.55.1"
  },
  "engines": {
    "node": ">=20"
  }
}
